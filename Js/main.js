//For the start game
function StartGame() {
    let name = document.createElement("textarea".nickname);
    name.style.position = "absolute";
    name.style.left = "30px";
    name.style.top = "0";
    name.style.placeholder = "chose a nickname";
    document.body.appendChild(name);
    name.textContent = t;
    name.select();
    function StartGame() {
    Name
}
}
